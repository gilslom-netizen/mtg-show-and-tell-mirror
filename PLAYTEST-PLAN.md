# תוכנית עבודה — משוב הפלייטסט של 26.8.26

מקור: המשחק המשותף של גיל ויעקב ב-26.8 (חדר `UBJTJ`), התמלול המלא של השיחה,
ומסמך הסיכום [`PLAYTEST-FEEDBACK.md`](PLAYTEST-FEEDBACK.md) שנמצא לצד הקובץ
הזה. הסיכום אומר *מה נאמר*; המסמך הזה אומר *מה לעשות* — כולל דברים שמופיעים
בתמלול ולא בסיכום (דיווח ההחלטות הנסתרות ובועת הדיבור, סעיף 3).

**מה המסמך הזה כן ומה הוא לא.** כל פריט כאן עבר בדיקה בקוד לפני שנכתב — לא
העברתי משפט מהשיחה לרשימת מטלות בלי לפתוח את הקובץ. איפה ששחזרתי את התקלה,
כתוב **מאומת** ואיך. איפה שלא הצלחתי, כתוב **לא שוחזר** ומה חסר כדי לשחזר.
שום דבר כאן עוד לא מומש.

**סימון עדיפות**: 🔴 חוסם · 🟠 חשוב · 🟡 שיפור · ⚪ פתוח לדיון

---

## 0. לפני הכל: הפלייטסט רץ על גרסה בת 72 קומיטים 🔴

זה לא פריט ברשימה, זה ההקשר של כל הרשימה.

`Production Branch` ב-Vercel מצביע על `claude/magic-online-game-y9jke0` שנתקע
ב-`bb4eace`. `main` נמצא 72 קומיטים קדימה, ולענף ההוא **אין אף קומיט משלו** —
הוא אב קדמון ישיר. כלומר האתר שיעקב שיחק עליו לא הכיל:

| | קיים ב-`main` | היה באתר |
|---|---|---|
| מוליגן ראשון חינם | ✅ | ❌ |
| ארבע ההערטות (`data/errata.json`) | ✅ | ❌ |
| Commandeer / Force of Negation / Mindbreak Trap / Pact of Negation | ✅ | ❌ |
| Jace's Erasure שבאמת עושה מיל | ✅ | ❌ |
| חמשת הקלפים החדשים בקיוב | ✅ | ❌ |
| 14 ערימות בדראפט | ✅ | ❌ |

אימות: `git cat-file -e bb4eace:data/errata.json` → חסר, וכן לכל השאר.

**מה זה אומר בפועל.** "אין free mulligan" ו-"למה הקלף הזה לא מהערטה" הם לא
באגים — הם גרסה ישנה. מצד שני, כל מה שכן נמצא ברשימה למטה **אומת מול `main`
הנוכחי**, אז הוא אמיתי גם היום.

**מה לעשות**: או לשנות `Settings → Git → Production Branch` ל-`main` (שינוי
אחד, רק גיל יכול), או fast-forward של הענף הישן ל-`main` (בטוח לחלוטין, כי
הוא אב קדמון). **עד שזה נסגר, כל פלייטסט נוסף בודק קוד ישן.**

---

## 1. קלף בלי סקריפט ניתן להטלה — ולא עושה כלום 🔴

> "אתה יכול לראות בלוג גם שהטלתי את הקלף אבל לא כתוב שזרקת משהו… לדעתי פשוט
> הקלף לא עשה כלום"

**מאומת, ובדיוק כמו שתואר.** הרצתי Thoughtseize במנוע:

```
offered as castable: [ 'Cast Thoughtseize' ]
log after casting:
    casts Thoughtseize
p2 hand still: 0
```

הקלף מוצע, נשלמת עלותו, הוא נפתר, נכנס לבית הקברות — ולא קורה כלום. הסיבה
ב-`resolveSpell` (`src/engine/game.ts`): `if (script?.resolve)`. אין סקריפט,
אין `resolve`, אין תלונה.

זה השורש של רוב תלונות ה"הקלף לא עבד". **57 מתוך 68 קלפי הקיוב הם כאלה.**

### 1.1 מה לעשות מיד — שהמנוע יסרב בשקט להעמיד פנים

שלוש אפשרויות, לפי סדר העדיפות שאני ממליץ עליו:

**א. לא להציע קלף לא ממומש כלל** (`enumerateLegalActions`). הקלף בשלב הזה הוא
פיסת נייר: אי אפשר להטיל אותו, בדיוק כמו שאי אפשר להטיל קלף שאין לך מאנה אליו.
היד תסמן אותו כ"לא ניתן לשחק — עדיין לא ממומש", בדיוק כמו "not enough mana".
היתרון: **אי אפשר לאבד משחק בגלל זה.** החיסרון: קלף שנדרפט הופך לחסר ערך במקום
לחסר תוכן.

**ב. להציע, אבל לכתוב בלוג בקול רם** — `casts Thoughtseize (not implemented — nothing happens)`.
זול מאוד, ופותר את "לא הבנתי אם זה עבד". לא פותר את "בזבזתי קלף".

**ג. לחסום בבניית הדק** — לא לתת לנעול דק שיש בו קלף לא ממומש.

**ההמלצה שלי: א' + ב' ביחד.** לא ניתן להטלה, וגם אם משהו יעקוף — הלוג אומר.
ובנוסף, אזהרה בבניית הדק (לא חסימה), כדי שהבחירה תהיה מודעת.

### 1.2 והתיקון האמיתי — לממש את הקלפים

הפער נמדד היום בבדיקה (`smoke.test.ts`, "knows which drafted cards are not
playable yet"). **57 קלפים.** אלה שנראו במשחק או שהוזכרו בשיחה, לפי סדר:

1. **Thoughtseize** — נשחק במשחק ולא עשה כלום
2. **Utopia Sprawl** — "גם הוא לא עבד לי חלק" (וראו 2.7 — צריך אוראות)
3. **Deathrite Shaman** — `producedMana` ריק, אין סקריפט, לא עושה כלום
4. **Chrome Mox** — גרוע מזה, ראו 2.1
5. **Cavern of Souls** — ראו 2.1
6. **Snapcaster Mage**, **Lier** — פלאשבאק, מנגנון שלם שלא קיים
7. **Mystical Dispute**, **Spell Pierce**, **Miscast**, **Dovin's Veto**,
   **Flusterstorm**, **Memory Lapse**, **Pyroblast** — קאונטרים; כולם וריאציות
   על `ctx.counterSpell` שכבר קיים, אז זו הקבוצה הזולה ביותר להתחיל בה
8. השאר

**הצעה מעשית**: לא לנסות לסגור 57 בבת אחת. לעבוד בקבוצות לפי מנגנון משותף
(קאונטרים → מאנה → הגלייה/דיסקארד → פלאשבאק → אוראות), ואחרי כל קבוצה להריץ
פלייטסט. הבדיקה ב-`smoke.test.ts` כבר סופרת את הפער — היא ההתקדמות.

---

## 2. באגים מאומתים

### 2.1 Chrome Mox ו-Cavern of Souls נותנים מאנה חינם 🔴

**מאומת:**

```
Chrome Mox      produced=[B,G,R,U,W]  script=NO
Cavern of Souls produced=[B,C,G,R,U,W] script=NO
```

`producedManaFor()` ב-`src/engine/oracle.ts` בודקת `/\{T\}: Add /` בטקסט
ומתעלמת מהתנאי שלפניו. Chrome Mox אומר "{T}: Add one mana of any of the exiled
card's colors" — **בלי Imprint אין צבעים בכלל**, אבל המנוע מציע חמישה. זה מוקס
חינמי שגם מסדר צבעים, בלי להגלות כלום. Cavern of Souls מקבל פטור אוטומטי כקרקע
ומאבד את ההגבלה "רק ליצור מהסוג שנבחר".

זה **חמור יותר** מקלף שלא עושה כלום: קלף שעושה משהו חזק שהוא לא אמור לעשות.

**תיקון**: לצמצם את הבדיקה ב-`producedManaFor` כך שתדרוש שהיכולת תהיה ללא תנאי
— בפועל, שהשורה תתחיל ב-`{T}: Add` ולא תכיל התניה. Chrome Mox ייפול לקטגוריה
"לא ממומש" (סעיף 1) עד שייכתב לו סקריפט אמיתי עם Imprint. Cavern of Souls יצטרך
סקריפט שבוחר סוג יצור בכניסה.

**ובכיוון ההפוך**: `isPlayable()` ב-`src/client/DeckBuilder.tsx` שואלת "יש
סקריפט?" ולכן מסמנת גם את **Birds of Paradise** ו-**Mox Emerald** כלא ממומשים —
בזמן שהם עובדים מצוין (כל הקלף שלהם הוא היכולת שמגיעה מ-`produced_mana`).
זה בדיוק ה"לחלק מהקלפים הוא לא שם אזהרה" שיעקב שם לב אליו. השאלה הנכונה היא
"האם משהו בקלף הזה בפועל עובד?" ולא "האם יש קובץ סקריפט?".

### 2.2 תיבת ההצעה בדראפט מציגה מספר תקוע מהערימה הקודמת 🟠

> "אבל הברירת מחדל עדיין היה 4" (בזמן שגיל הציע 1)

**מאומת בקוד.** `BidControls` ב-`src/client/Draft.tsx`:

```ts
const [amount, setAmount] = useState(min);
useEffect(() => {
  setAmount((a) => (a < min || a > max ? Math.min(min, max) : a));
}, [min, max]);
```

`useState(min)` רץ פעם אחת בלבד — הרכיב לא מתפרק בין ערימות. ה-`useEffect`
מתקן רק כשהמספר **נמוך מדי או גבוה מדי**. אז אם הצעת 4 בערימה קודמת, המספר 4
נשאר בתיבה גם בערימה הבאה שבה המינימום הוא 2 — ונראה כמו "ברירת המחדל".

זה מסביר את כל הבלבול בתחילת השיחה: יעקב הביט במספר שהמערכת הציגה **בתיבה
שלו**, הניח שזו ההצעה של גיל, וחשב שגיל שם 3.

**תיקון**: לאפס את התיבה בכל מכרז חדש. הכי פשוט — `key={draft.pileNumber}` על
הרכיב, שגורם ל-React לבנות אותו מחדש עם `useState(min)` נכון. או `useEffect`
שמאזין ל-`pileNumber` ומחזיר ל-`min`.

### 2.3 רווח מדלג על אזהרת המאנה הצפה 🟠

> "וגם עכשיו נעלם לי המאנה, כי העברתי פייז בטעות"

**מאומת.** יש הגדרה `warnOnFloatingMana` (ברירת מחדל: דולקת), אבל היא ממומשת
**רק** בכפתור Pass ב-`BottomBar` (`src/client/App.tsx`, `doPass`). מקש הרווח
ב-`hooks.ts` שולח ישירות:

```ts
case ' ':
case 'F2':
  if (view && canAct(view, viewer)) send({ t: 'passPriority' }, viewer);
```

בלי שום בדיקה. כלומר הדרך המהירה והנפוצה ביותר להעביר תור היא בדיוק זו
שמתעלמת מהאזהרה.

**תיקון**: להוציא את `doPass` (כולל בדיקת המאנה הצפה) למקום אחד שגם המקלדת
וגם הכפתור קוראים לו. זה הכלל הכללי — קיצור מקלדת צריך לעבור באותו נתיב כמו
הכפתור, אחרת כל בדיקה בכפתור היא בדיקה שקל לעקוף בלי לשים לב.

### 2.4 כפתור בית הקברות נעלם כשהוא ריק 🟡

> "אגב לדעתי צריך להיות אפשרות ללחוץ על היארד ולראות מה יש שם"

**מאומת.** הפיצ'ר קיים במלואו (`Yards` ב-`src/client/Board.tsx`), אבל:

```ts
if (gy.length === 0 && ex.length === 0) return null;
```

בתחילת משחק בית הקברות ריק, אז הכפתור פשוט לא שם — ומי שלא ראה אותו פעם אחת
לא יודע שהוא קיים.

**תיקון**: להציג תמיד, גם "Graveyard 0". פיצ'ר שמופיע רק אחרי שכבר היית צריך
אותו הוא פיצ'ר שאף אחד לא מגלה.

### 2.5 "ראש הדק" מציג "a card" במקום שם 🟠

> "מה שהוא מראה לי בחלון של הטופ של הדק לא נכון, כתוב לי פשוט 'קלף' ו'קלף'"

**מאומת.** `KnownTopPanel` ב-`src/client/ui.tsx` קורא `cardTitle(view, e.iid)`
בכל רינדור. `knownTop` אמור להיות **זיכרון של הלקוח** — מה שראית כשהקלף הוצג
לך — אבל השם נשלף מה-view החי, ו-`redact` כבר לא כולל את הקלף הזה. אז השם
מתאדה והופך ל-placeholder.

**תיקון**: לשמור את ה-`oracleId` בתוך `KnownTopEntry` ברגע הלמידה. זה גם
מבחינה עקרונית נכון יותר — זיכרון של שחקן הוא לא שאילתה על המצב הנוכחי.

### 2.6 Ponder לא מדווח אם ערבב 🟠

> "בעיקרון אמור להיות כתוב לי אם בחרת לערבב את הפונדר או לא" / "נכון, זה הוא
> באמת לא כותב"

**מאומת** — `src/engine/cards/cantrips.ts`, הסקריפט של Ponder לא קורא ל-`ctx.log`
בכלל. ראו סעיף 3, זה מקרה פרטי של בעיה כללית.

### 2.7 אוראות לא קיימות במנוע ⚪🟠

> "ממש בלתי אפשרי לראות את כל הלוח, הספראל שלך תופס את כל המקום כרגע"

**מאומת** — חיפוש `attachedTo` בכל `src/engine` מחזיר אפס תוצאות. אין מושג של
הצמדה. Utopia Sprawl (ו-Animate Dead, ו-Ashiok's Erasure שמגלה ספל) נפתרים
לשדה הקרב כפרמננטים עצמאיים.

זה גם באג כללים (האוראה לא עושה כלום) וגם בעיית תצוגה: היא תופסת משבצת מלאה
בשורת השדה במקום להיות מוצמדת לקרקע.

**מה זה דורש**: שדה `attachedTo?: IID` ב-`CardInstance`, טיפול בהתנתקות כשהמטרה
עוזבת, ותצוגה מוערמת בלוח. **זה שינוי מבני**, לא תיקון נקודתי — אבל בלעדיו כל
קלף Aura בקיוב לא יכול לעבוד. שווה להחליט אם הוא בסקופ לפני שמתחילים לממש
קלפים (סעיף 1.2).

---

## 3. מה שהמשחק לא מספר לך — ההחלטות הנסתרות 🟠

זו הבקשה החדשה של גיל, והיא הכי רחבה במסמך הזה. הניסוח שלו:

> "שניתן יהיה לראות עם ponder האם היריב ערבב או לא, כנ"ל גם סרביל לנד האם זרק
> או שמר או כל דבר כזה, שגם יישמר בלוג וגם אולי תעשה מין משהו כזה של בועת דיבור
> שיוצאת מהשחקן שאומרת מה בחר"

**הבעיה האמיתית היא לא Ponder.** היא שאין כלל שאומר מתי החלטה של שחקן צריכה
להיות גלויה. חלק מהקלפים כותבים ("Waterlogged Teachings: reveals X"), חלק לא
(Ponder), ואף אחד לא החליט מה הכלל.

### 3.1 הכלל שאני מציע

> **כל החלטה שהיריב זכאי לדעת עליה נכתבת בלוג ברגע שהיא נעשית — גם כשהתוכן
> נשאר סודי.**

ההבחנה היא בין **מה בחרת** לבין **שבחרת**. יריב לא זכאי לדעת אילו שני קלפים
החזרת עם Brainstorm. הוא כן זכאי לדעת שערבבת, כי הערבוב הוא מידע ציבורי לפי
הכללים — הספרייה השתנתה מול העיניים שלו.

הרשימה לפי הקלפים הקיימים:

| קלף/מקור | מה חייב להיכתב | מה נשאר סודי |
|---|---|---|
| **Ponder** | "ערבב" / "בחר לא לערבב" | הסדר החדש |
| **קרקעות סרביל** | "הכניס 1 לקבר" / "השאיר למעלה" | איזה קלף |
| **Brainstorm** | "החזיר 2 לראש" | אילו |
| **Planar Genesis** | "לקח קרקע לשדה" / "לקח קלף ליד" | איזה קלף |
| **Show and Tell** | כבר גלוי (שניהם חושפים) | — |
| **Dig / Bargain** | "לקח קלף" | איזה |
| **מוליגן** | כבר קיים | — |

**מימוש**: זו שורת `ctx.log` אחת בכל סקריפט. הבדיקה שתמנע נסיגה: לעבור על כל
`getScript(id).resolve` שמכיל `chooseCards` או `yesNo` ולוודא שיש בו `ctx.log`.
אפשר לכתוב את זה כבדיקה אמיתית שסורקת את הרג'יסטרי, לא כרשימה ידנית שתתיישן.

### 3.2 בועת הדיבור

הרעיון של גיל, ואני חושב שהוא נכון: הלוג הוא ארכיון, לא התראה. משהו שקרה
לפני שתי שניות צריך להופיע **על השחקן**, לא בעמודה בצד שצריך לקרוא.

**הצעה**: בועה קטנה שיוצאת מ-`PlayerBar` של השחקן שעשה את הפעולה, מציגה את
אותה שורת לוג בדיוק, ונעלמת אחרי ~3 שניות. אותו מקור נתונים — אם זה בלוג, זה
בבועה. כך אי אפשר שהם יסתרו זה את זה.

פרטים שכדאי להחליט מראש:
- **מה נכנס לבועה**: לא כל שורת לוג. רק החלטות של שחקן (לא "— Turn 3 —" ולא
  "resolves"). מוצע: לסמן שורות לוג בשדה `notable: true` ולהציג רק אותן.
- **כמה זמן**: ~3 שניות, או עד שהבאה מגיעה.
- **תור מהיר**: בתור אומנישנס יש עשרות שורות בשנייה. הבועה צריכה להציג את
  האחרונה ולא לנסות להדביק את כולן.
- **נגישות**: הבועה היא נוחות, הלוג הוא האמת. אסור שמידע יופיע רק בבועה.

---

## 4. בהירות — מה שקיים אבל לא נראה

כל הפריטים כאן הם רכיבים שכבר קיימים ופשוט לא בולטים מספיק. אין כאן קוד חדש,
יש עיצוב.

### 4.1 מי ראשון ומי שני — לא מוצג בכלל 🟠

> "זה לא מציג לי אם אני ראשון או שני במוליגן. זה די חשוב"

**מאומת**: `PlayerView` ב-`redact.ts` לא חושף `startingPlayer` בכלל, ו-
`MulliganDialog` לא מציג את זה בשום צורה.

**תיקון**: לחשוף את השדה ב-`redact`, ולהציג "You're on the play" /
"You're on the draw" בכותרת דיאלוג המוליגן — ורצוי גם ב-`PlayerBar` לאורך
המשחק.

**זה כנראה גם מסביר תלונה שנייה**: "הוא נותן לך לשלוף גם אם אתה ראשון".
בדקתי את המנוע — `if (!(s.turn === 1 && ap === s.startingPlayer)) this.draw(ap, 1)`
— הכלל **נכון**. יעקב כנראה לא ידע שהוא השני, אז השליפה התקינה שלו נראתה כמו
באג. תיקון 4.1 פותר גם את זה, בלי לגעת במנוע.

### 4.2 פריוריטי 🟡

> "כדאי שיהיה איזשהו חלון קופץ או מקום שאפשר לראות אצל מי פריורטי"

הצ'יפ קיים ב-`PlayerBar` אבל קטן. **הצעה**: להדגיש את **כל שורת השחקן** שמחזיק
פריוריטי (מסגרת/רקע), לא רק תווית קטנה. זה מידע שנקרא עשרות פעמים בתור.

### 4.3 שלב/פייז 🟠

> "התכוונתי להביא סרביל ולא הבנתי מה הפייז… יש מקום שכתוב בו איזה פייז זה?"

`PhaseTrack` קיים ומציג "Turn N · שלב", אבל לא בגודל שמאפשר לשחק לפיו.
זה גרם ליעקב **לאבד מאנה** (סעיף 2.3) כי לא ידע איפה הוא.

**הצעה**: פס שלבים גלוי עם השלב הנוכחי מודגש, כך שרואים גם איפה אנחנו וגם
מה בא אחרי. זה גם יעזור לסעיף 4.5.

### 4.4 הסטאק בטקסט בלבד 🟡

> "יהיה הרבה יותר נעים בעין אם תראה גם את הקלף שעל הסטאק ולא רק יהיה כתוב שמות"

**מאומת** — `StackPanel` ב-`ui.tsx` מרנדר `cardTitle(...)` כטקסט בלבד.

**תיקון**: תמונה קטנה לכל פריט. שים לב שפריט בסטאק יכול להיות **יכולת** ולא
קלף (טריגר של Hullbreaker) — לזה מתאימה תמונת קלף המקור עם תווית היכולת.

### 4.5 דילוגי שלב אוטומטיים ⚪

> "למה זה לא נתן לי להגיב באנד סטפ?… זה מערכת מבלבלת"

זו התנהגות מכוונת (המנוע מדלג על שלב שאין בו מה לעשות). לא באג — אבל
"המשחק קפץ קדימה בלי להסביר" מקשה ללמוד את הפורמט.

**הצעה**: שורת לוג עדינה כשמשהו מדולג ("nothing to do in combat — skipped").
ההגדרות כבר מאפשרות לכבות את הדילוגים; מי שלא יודע שהן קיימות לא ימצא אותן.

### 4.6 המספרים במסך הדראפט 🟠

זה קשור לוויכוח על השווי הממוצע (סעיף 5.3). על המסך מופיעים היום שני מספרים
קרובים מאוד: `pile.number / pilesTotal` ("4 / 17") ו-`pilesRemaining` ("11").
קל מאוד לקרוא את הראשון כ"נשארו 4".

**תיקון**: תוויות מפורשות. "Pile 4 of 17" ו-"11 piles left" — לא מספרים
עירומים.

---

## 5. נבדק — לא באג, או לא שוחזר

### 5.1 מונה התורות ✅ תקין
המונה סופר **סיבובים**, לא תורות של שחקן בודד
(`if (nextActive === s.startingPlayer) s.turn++`). זה בדיוק מה שגיל תיאר שהוא
רוצה. אם זה עדיין נראה שגוי — צריך צילום מסך.

### 5.2 שליפה של השחקן הראשון ✅ תקין
ראו 4.1. הכלל ממומש נכון; חסר רק שיידעו מי ראשון.

### 5.3 השווי הממוצע של הערימה ⚠ לא שוחזר

> "יש עדיין 4 חבילות ויש לנו ביחד 14 מטבעות, לא יכול להיות שהממוצע יותר מ-4"

הרצתי דראפט אמיתי ומדדתי:

```
phase   | pileNo | coins    | cardsRemaining | pilesRemaining | average
start   | 1      | 10+10=20 | 56             | 14             | 1.43
after 1 | 2      | 8+10=18  | 52             | 13             | 1.38
after 3 | 4      | 6+8=14   | 44             | 11             | 1.27
```

`cardsRemaining` **כן** כולל את הערימה שעל השולחן. בדקתי גם את הקוד **כפי
שהיה בגרסה המותקנת** (`bb4eace`) — זהה בדיוק. אז החישוב תקין בשתי הגרסאות,
ואת 4.7 אני לא מצליח לייצר.

ההסבר הסביר ביותר הוא בלבול בין שני המספרים על המסך (סעיף 4.6). אבל אני לא
רוצה לסגור את זה על ניחוש.

**ההצעה האמיתית**: דראפט הוא כבר `(seed, action log)` — כתוב במפורש בראש
`src/draft/draft.ts`. **חסר רק לשמור את הלוג הזה**, בדיוק כמו שהמשחקים
נשמרים היום ב-`src/client/history.ts`. אז טענה כזאת נבדקת בשחזור במקום
בוויכוח מהזיכרון. זה גם מה שאיפשר למצוא ולתקן דברים בשני המשחקים הקודמים
שגיל שלח.

---

## 6. פיצ'רים חדשים

### 6.1 להאריך סדרה אחרי שהוכרעה 🟡

> "כדאי להוסיף אפשרות שהוא ישאל בסוף המאצ' אם להאריך אותה בכמה משחקים… כי
> הרבה פעמים מחליטים להאריך ל-5 אחרי 3 ולא בהתחלה"

היום `bestOf` נקבע רק בפתיחת החדר. **הצעה**: במסך סיום המאצ', כפתור
"עוד 2 משחקים" שמאריך את הסדרה הקיימת. שים לב שזה דורש הסכמה של שני הצדדים
— אחרת המנצח יכול להאריך לבד עד שינצח. מוצע: אחד מציע, השני מאשר.

### 6.2 אישור לפני נעילת דק 🟡

> "אוי, הוא גם לא שומר את הסיידבורד" / "כן בדיוק עשיתי בטעות submit לדק הבסיסית"

לא מצאתי באג "לא שומר" — זה נראה כמו submit מוקדם בטעות. הכפתור נועל בלי לשאול.

**תיקון**: אישור קצר שמראה מה בדיוק ננעל ("60 קלפים, מתוכם 4 מהדראפט: …").
זול, ומונע בדיוק את הטעות שקרתה.

---

## 7. עיצוב הקיוב — פתוח, דורש החלטה שלכם ⚪

לא נוגע בזה בלי הכרעה מפורשת:

- **Krosan Grip / Nature's Claim** — גיל הציע לחתוך; יעקב חלק. לא הוכרע.
- **Snapcaster Mage** — גיל הציע להוזיל ב-1–2. לא הוכרע.
- **Deathrite Shaman** — נוסף; יעקב הביע חשש שהוא איום גם בקונטרול. שווה מעקב.
- **ארט חסר**: Steam Vents, Meticulous Archive, Thundering Falls — שלושתן עדיין
  על ברירת המחדל של Scryfall. יעקב אמר ספציפית שה-Steam Vents "לא נראה משהו".
  צריך set + collector number לכל אחת.

---

## 8. סדר עבודה מוצע

**קודם כל — בלי זה כל השאר מתבזבז:**
1. לתקן את ה-Production Branch (סעיף 0). כל פלייטסט על קוד ישן הוא בזבוז זמן
   של שניכם.

**גל א' — עצירת נזק (הכל קטן, הכל מאומת):**
2. Chrome Mox / Cavern of Souls לא נותנים מאנה חינם (2.1)
3. קלף לא ממומש לא ניתן להטלה + נכתב בלוג (1.1)
4. `isPlayable` בדקבילדר שואלת את השאלה הנכונה (2.1)
5. תיבת ההצעה מתאפסת בכל ערימה (2.2)
6. רווח עובר באותה בדיקה כמו הכפתור (2.3)

**גל ב' — מידע (הכי הרבה החזר על ההשקעה):**
7. מי ראשון/שני (4.1)
8. `ctx.log` לכל החלטה נסתרת + הבדיקה שסורקת את הרג'יסטרי (3.1)
9. ראש הדק זוכר שמות (2.5)
10. כפתור בית קברות תמיד גלוי (2.4)
11. תוויות במסך הדראפט (4.6)

**גל ג' — נוחות:**
12. בועת דיבור (3.2)
13. פס שלבים בולט (4.3), הדגשת פריוריטי (4.2)
14. תמונות בסטאק (4.4)
15. אישור נעילת דק (6.2), הארכת סדרה (6.1)

**גל ד' — הגדול:**
16. שמירת לוגי דראפט (5.3)
17. אוראות במנוע (2.7) — החלטה לפני מימוש
18. מימוש 57 הקלפים בקבוצות (1.2)

---

## נספח: מה אומת ואיך

| טענה | שיטה | תוצאה |
|---|---|---|
| קלף בלי סקריפט ניתן להטלה ולא עושה כלום | הרצת Thoughtseize במנוע | ✅ שוחזר |
| Chrome Mox נותן 5 צבעים בלי Imprint | `oracleByName('Chrome Mox').producedMana` | ✅ שוחזר |
| Birds/Mox Emerald עובדים למרות האזהרה | אותה בדיקה | ✅ שוחזר |
| תיבת ההצעה תקועה | קריאת `BidControls` | ✅ מנגנון זוהה |
| רווח מדלג על אזהרת מאנה | קריאת `hooks.ts` מול `App.tsx` | ✅ שוחזר |
| Yards מוסתר כשריק | `if (gy.length === 0 …) return null` | ✅ שוחזר |
| ראש הדק מציג "a card" | קריאת `KnownTopPanel` | ✅ מנגנון זוהה |
| Ponder לא כותב ללוג | קריאת `cantrips.ts` | ✅ שוחזר |
| אוראות לא ממומשות | חיפוש `attachedTo` בכל המנוע | ✅ שוחזר (אפס תוצאות) |
| מונה תורות שגוי | קריאת `game.ts` | ❌ תקין |
| שליפה של הראשון | קריאת `game.ts` | ❌ תקין |
| שווי ממוצע שגוי | הרצת דראפט + השוואה ל-`bb4eace` | ⚠ לא שוחזר |
| האתר מריץ קוד ישן | `git cat-file -e bb4eace:…` | ✅ 72 קומיטים |
