# יריב AI ל־Show and Tell Mirror — מסמך תכנון

מסמך תכנון לבניית ואימון יריב מלאכותי שאפשר לשחק נגדו. שלב א' — המשחק הבסיסי (המירור). שלב ב' — הדראפט.

> **המסקנה בשורה אחת:** בזכות שלוש עובדות מדודות — **גורם הסתעפות ממוצע של 3**, **25 קלפים ייחודיים בלבד**,
> ו**מנוע דטרמיניסטי לחלוטין** — זה משחק קטן בהרבה ממה שהוא נראה, וסוכן חזק מאוד נמצא בהישג יד בלי אשכול GPU.
> הצוואר האמיתי הוא **מהירות המנוע** (207µs להחלטה, מתוכם 99µs בזבוז שניתן להסיר מיד),
> ובדראפט — **67 מתוך 69 קלפי המאגר עדיין חסרי סקריפט**, ולכן אין ממה ללמוד את ערכם.

---

## תוכן עניינים

1. [מה מדדתי לפני שתכננתי](#1-מה-מדדתי-לפני-שתכננתי)
2. [אפיון הבעיה — ולמה היא קלה ממה שנדמה](#2-אפיון-הבעיה--ולמה-היא-קלה-ממה-שנדמה)
3. [עקרונות מנחים](#3-עקרונות-מנחים)
4. [סולם הגישות — חמישה שלבים, כל אחד ניתן למשלוח](#4-סולם-הגישות--חמישה-שלבים-כל-אחד-ניתן-למשלוח)
5. [תשתית: ממשק הסוכן, self-play, זירת הערכה](#5-תשתית-ממשק-הסוכן-self-play-זירת-הערכה)
6. [האצת המנוע — התנאי לכל השאר](#6-האצת-המנוע--התנאי-לכל-השאר)
7. [ייצוג המצב (encoding)](#7-ייצוג-המצב-encoding)
8. [מרחב הפעולות ופונקציית המדיניות](#8-מרחב-הפעולות-ופונקציית-המדיניות)
9. [חיפוש תחת מידע חלקי](#9-חיפוש-תחת-מידע-חלקי)
10. [ארכיטקטורת הרשת](#10-ארכיטקטורת-הרשת)
11. [לולאת האימון](#11-לולאת-האימון)
12. [מודל אמונה ומודל יריב](#12-מודל-אמונה-ומודל-יריב)
13. [נקודות שהן באמת תורת משחקים](#13-נקודות-שהן-באמת-תורת-משחקים)
14. [הערכה: איך יודעים שהוא באמת חזק](#14-הערכה-איך-יודעים-שהוא-באמת-חזק)
15. [דרגות קושי וחוויית שחקן](#15-דרגות-קושי-וחוויית-שחקן)
16. [פריסה: להריץ את זה בדפדפן](#16-פריסה-להריץ-את-זה-בדפדפן)
17. [שלב ב' — הדראפט](#17-שלב-ב--הדראפט)
18. [תקציב חישוב ולוח זמנים](#18-תקציב-חישוב-ולוח-זמנים)
19. [סיכונים ומלכודות](#19-סיכונים-ומלכודות)
20. [נספח: החלטות שכבר סגורות](#20-נספח-החלטות-שכבר-סגורות)

---

## 1. מה מדדתי לפני שתכננתי

כל מספר כאן נמדד על הקוד הקיים, על ליבה אחת, לפני שכתבתי שורה מהתוכנית. הם מה שקובע אילו גישות בכלל
אפשריות, ולכן הם בראש המסמך ולא בנספח.

| מדד | ערך | למה זה קובע משהו |
|---|---|---|
| משחק אקראי מלא | **38 מ"ש** (26 משחקים/שנייה, ליבה אחת) | קובע את קצב ה־self-play |
| החלטה בודדת במנוע | **207 µs** | הצוואר של כל חיפוש |
| מתוכם: `JSON.stringify(state)` ל־snapshot של Esc | **99 µs (48%)** | ניתן להסרה מיידית במצב headless |
| שכפול מצב (JSON round-trip) | 234 µs — **יקר מהחלטה שלמה** | פוסל MCTS שמשכפל מצב לכל צומת |
| החלטות למשחק | **~200** | אופק ארוך |
| גורם הסתעפות בעדיפות | **ממוצע 2.99**, חציון 2, p95 6, מקסימום 16 | קטן להפליא — חיפוש זול |
| גורם הסתעפות בבחירות | ממוצע 4.6, p95 10, מקסימום 53 | ה־53 הוא בחירת קלפים מתוך רשימה |
| אורך משחק | **14.1 סיבובים** בממוצע, p95 21 | קצר |
| קלפים ייחודיים במיין | **25** | אין צורך בהכללה על טקסט קלפים |
| מסד הקלפים כולו | 95 | |
| קלפים עם סקריפט | **27** | המירור שלם; המאגר לא |
| מאגר הדראפט | **69 קלפים, 2 עם סקריפט** | חוסם למידה מבוססת־משחק בדראפט |
| `redact()` | 8–62 µs | לא צוואר |
| `legalActions()` | 0–40 µs | לא צוואר |
| מצב מוצפן (view) באמצע משחק | 12.5 KB | |

**שתי מסקנות מיידיות:**

1. גורם הסתעפות של 3 עם 200 החלטות הוא פרופיל של משחק שבו **חיפוש עדיף על אינטואיציה**. עץ של עומק 10
   הוא בערך 60,000 צמתים — כלום. הקושי אינו הרוחב אלא **המידע החלקי** והאופק.
2. 207µs להחלטה זה יקר בשביל חיפוש. חצי מזה הוא snapshot שנועד לכפתור Esc בממשק ואין בו שום צורך
   ב־self-play. §6 מפרט את זה ואת מה שמעבר.

---

## 2. אפיון הבעיה — ולמה היא קלה ממה שנדמה

**הסיווג הפורמלי:** משחק סכום־אפס לשני שחקנים, מידע חלקי, סטוכסטי (ערבוב), אופק ארוך, פעולות סימולטניות
בנקודה אחת (Show and Tell).

זה נשמע כמו פוקר או StarCraft. הוא לא, וההבדל הוא הדבר החשוב ביותר במסמך הזה:

### 2.1 אין אי־ודאות לגבי הדק

**שני השחקנים משחקים את אותם 60 הקלפים, וזה ידוע.** ב־MTG אמיתי חצי מהקושי הוא להסיק איזה דק היריב
משחק. כאן אין מה להסיק. מרחב האמונה אינו "התפלגות על דקים אפשריים" אלא **התפלגות על תמורות של רב־קבוצה
ידועה** — ואת זה אפשר לייצג במדויק.

מבחינה מעשית: אם אני יודע שביריב יש 4 Mana Drain, ראיתי אותו משחק 2, ויש 3 בקברות שלו — אני יודע
בוודאות שאין לו עוד. הסקה כזאת היא אריתמטיקה, לא ניחוש. סוכן שעושה אותה נכון כבר מנצח את רוב האנשים.

### 2.2 המידע הנסתר הוא בדיוק ארבעה דברים

1. סדר הספרייה של כל שחקן.
2. תוכן היד של היריב (רב־קבוצה שמשלימה את מה שנראה).
3. הבחירה הסודית ב־Show and Tell, עד לחשיפה.
4. הכוונה של היריב — האם ה־mana הפתוח הוא Mana Drain או בלוף.

(1) ו־(2) קשורים: היד והספרייה יחד הן חלוקה של מה שנשאר. (3) הוא משחק מטריצה זעיר. (4) הוא היחיד
שבאמת דורש חשיבה תורת־משחקית.

### 2.3 רוב ההחלטות אינן החלטות

חציון גורם ההסתעפות הוא **2** — כלומר "pass" ועוד דבר אחד. שכבת ה־auto-pass הקיימת בלקוח כבר מסננת
את הרוב. המשחק מוכרע במספר קטן של צמתים אמיתיים:

- **מוליגן** (החלטה 1, משפיעה על ~15% מהתוצאה בדק קומבו).
- **הבחירה ב־Show and Tell** — משחק מטריצה סימולטני של 3–5 אפשרויות לכל צד.
- **האם לקאונטר** — כאן חיים הבלוף וה־information.
- **מתי ללכת על הקומבו** מול להחזיק תשובה.
- **סדר הפעולות בתור הקומבו** — כאן חיפוש שולט ואדם טועה.

תכנון שמפזר מאמץ שווה על כל 200 ההחלטות מבזבז אותו. **חלוקת תקציב חיפוש לפי חשיבות הצומת** היא
אופטימיזציה ממדרגה ראשונה כאן, לא כוונון עדין. §9.4.

### 2.4 מה זה אומר על גודל המודל

25 קלפים ייחודיים פירושם ש**אין צורך בהכללה על טקסט קלפים**. אין "מה עושה קלף שלא ראיתי". אפשר לתת
לכל שם קלף embedding ייעודי ולסיים. זו הסיבה שרשת של ~200K פרמטרים סבירה כאן, במקום מאות מיליונים.
זה גם מה שהופך פריסה בדפדפן לטריוויאלית (§16).

---

## 3. עקרונות מנחים

**ע1 — הסוכן רואה בדיוק מה ששחקן רואה.** הוא צורך `redact(state, seat)` ותו לא, ופועל דרך
`legalActions` / `pendingChoice` כמו כל לקוח. זה לא הגינות בלבד: זה מה שהופך את הסוכן לבר־פריסה
בדפדפן של השחקן, ומה שמונע ממנו ללמוד מדיניות שנשענת על מידע שלא יהיה לו בזמן משחק. **נאכף בבדיקה**,
לא בהסכמה — `RandomBot` הקיים כבר מכבד את זה וזו התבנית.

**ע2 — התגמול הוא ניצחון/הפסד בלבד.** בלי reward shaping ("נזק שנגרם", "קלפים שנשלפו"). בדק קומבו
כל שיפינג כזה מעוות: לגרום נזק זה לא המטרה, לפתור את המשחק זו המטרה. שיפינג נכנס כ**ראשי עזר**
(auxiliary heads) שמשפרים ייצוג בלי לזהם את המדיניות — §11.4.

**ע3 — כל שלב נשלח לבד.** אין "השלב הבא יתקן". שלב 1 (היוריסטיקה) הוא יריב שאפשר לשחק נגדו עוד באותו
שבוע, וגם ה־baseline שמולו מודדים לנצח.

**ע4 — מדידה לפני אופטימיזציה, וגם לפני ארכיטקטורה.** §1 היא הדגמה; זה נמשך לכל אורך הפרויקט.

**ע5 — סוכן שאי אפשר למדוד את ניצולתו (exploitability) הוא סוכן שלא יודעים אם הוא חזק.** במשחק מידע
חלקי, ניצחון מול היוריסטיקה לא אומר כמעט כלום. §14.

---

## 4. סולם הגישות — חמישה שלבים, כל אחד ניתן למשלוח

| שלב | מה | מאמץ | חוזק צפוי | תלוי ב |
|---|---|---|---|---|
| **0** | תשתית: ממשק סוכן, self-play headless, זירת Elo | 2–3 ימים | — | — |
| **1** | יוריסטיקה כתובה ביד | 3–5 ימים | מנצח שחקן מתחיל | 0 |
| **2** | חיפוש דטרמיניזציה (PIMC) | 3–4 ימים | **מנצח רוב בני האדם** | 0, 1, 6 |
| **3** | IS-MCTS | שבוע | חזק | 2 |
| **4** | מדיניות+ערך נלמדים (AlphaZero over information sets) | 3–5 שבועות | חזק מאוד | 3, 7, 10, 11 |
| **5** | זיקוק אמונה + פתרון תת־משחקים (CFR) | 2–3 שבועות | קרוב לאופטימלי בצמתים הקריטיים | 4, 12, 13 |

**ההמלצה:** לעצור אחרי 2 ולראות. שלב 2 על משחק בגורם הסתעפות 3 הוא חזק בצורה מפתיעה, והוא זול.
שלבים 4–5 מוצדקים אם המטרה היא "המקסימום שאפשר", וזו אכן המטרה שהוגדרה — אבל 2 הוא נקודת ההחלטה
הנכונה, לא ההתחלה.

### 4.1 מה כבר מומש — שלבים 0 ו־1

שלב 0 ושלב 1 בנויים ועובדים, למשחק הבסיסי בלבד (המירור; הדראפט לא נגע). הקוד יושב ב־`src/ai/`:

| קובץ | מה |
|---|---|
| `agent.ts` | ממשק `Agent` — `act` / `respond` / `chooseFirst` / `sideboard?`, מעל `PlayerView` בלבד |
| `view.ts` | קריאת ה־view: הפיכת רשימות מזהים לעובדות שהחלטה צריכה |
| `cards.ts` | כל הידע על 25 הקלפים — ערך, איום, דירוג Show and Tell, איכות קרקע |
| `random.ts` | `RandomAgent` — רצפת הסולם, דרך view ולא דרך state |
| `heuristic.ts` | שלב 1 |
| `arena.ts` | `playGame` / `playMatch` / `replayRecord`, ו־`GameRecord` כ־(seed, log) |
| `elo.ts` | Elo + רווח סמך Wilson מעל **זוגות**, לא מעל משחקים |
| `series.ts`, `worker.ts` | ריצה מרובת־חוטים על זוגות מראה |
| `bench.ts` | המדידות של §1, כפקודה |

שתי פקודות חדשות:

```bash
npm run ai:bench                                           # המספרים של §1
npm run ai:arena -- --a heuristic --b random --games 2000  # Elo עם רווח סמך
```

**התוצאה שנמדדה:** היוריסטיקה מנצחת את `random` ב־**97.7%** מ־2,000 משחקים
(רווח סמך 96.5%–98.4%, כ־**+647 Elo**, מובהק). מול עצמה: 300–300, 50.0% מדויק, וזה בדיוק מה
שזוגות מראה אמורים לתת — אישור שהזיווג מבטל את יתרון הפתיחה ולא ממצע עליו.

**שלושה דברים שהמדידה גילתה ולא היו בתוכנית:**

1. **הדק הורג את עצמו הרבה לפני שהיריב מספיק.** עם Omniscience על השולחן כל קלף ביד חינם, ולכן
   סוכן שמדרג פעולות פשוט מטיל את כולן — ארבע Atraxa שכל אחת חושפת עשרה ולוקחת עד שמונה זה 32
   קלפים לבד. הגרסה הראשונה שאבה את כל 60 הקלפים תוך 3–6 סיבובים ומתה בשליפה הבאה בלי להפסיד
   לאף אחד. התיקון אינו מספר קסם אלא **תנאי**: קלף שמחפש קלפים שווה את מחירו רק כל עוד יש מה
   לחפש. ברגע ש־Show and Tell ופרמננט נמצאים ביד עם שלוש קרקעות, או ש־Omniscience בשטח עם דרך
   לנצח — כל Brainstorm נוסף הוא קלף מהספרייה שהיא הדבר היחיד שיכול לנצח את הדק הזה. (`worthDrawing`
   ב־`heuristic.ts`.)
2. **במירור יש לולאה אינסופית אמיתית.** שני Hullbreaker Horror מול שני Omniscience מחזירים זה
   לזה את ה־Mana Drain לנצח: הקלף נוחת ביד שלא עולה כלום לצאת ממנה, שום קלף לא מושקע, שום חיים
   לא זזים. הזירה מצאה את זה בכך שסירבה לסיים 65 משחקים מתוך 600. המסקנה היא ידע על הפורמט —
   **להחזיר לחש ליד היא לא נטרול כשליריב יש Omniscience** — ולא באג. שווה לזכור לשלב 5: זו נקודה
   שבה החוקים עצמם (CR 104.4b) מדברים על תיקו.
3. **הסנאפשוט של Esc באמת עולה חצי החלטה, והמקביליות לא מגיעה למה שהתוכנית הניחה.** §6.1 אושר
   (`undoable: false`). §6.4 לא: ראו §18.1 למטה.

### 4.2 שלב 2 — PIMC: בנוי, נכון, **ועובד**

> **המדידה:** `pimc:4` מנצח את היוריסטיקה של שלב 1 ב־**61.0%** מ־400 משחקים
> (רווח סמך 54.1%–67.5%, **+78 Elo**, מובהק ב־95%). זה עם החיפוש **הדק ביותר** —
> ארבע דטרמיניזציות. §4.2ג.

| קובץ | מה |
|---|---|
| `determinize.ts` | `PlayerView` → `GameState` מלא + **בדיקת נאמנות בזמן ריצה** |
| `pimc.ts` | הסוכן: דטרמיניזציות × מועמדים, rollouts, CRN, הקצאת תקציב לפי §9.4 |
| `matrix.ts` | פתרון משחק מטריצה בסכום אפס ב־fictitious play |
| `showandtell.ts` | §13.1 — הבחירה הסודית כמשחק מטריצה |

#### 4.2א הדבר שכל שלב 2 עומד עליו: שחזור עמדה מ־view

סוכן רואה `PlayerView` בלבד (ע1), ו־PIMC צריך `GameState` שאפשר לשחק עליו קדימה. ה־view נבנה
כדי **להסתיר**, לא כדי לחזור — ולכן השחזור הוא העבודה האמיתית כאן. מה שאי אפשר לקרוא, אפשר להסיק,
וכל הסקה כזאת כתובה במקום שבו היא נעשית:

- `startingPlayer` — תורות מתחלפים מתור 1, אז מי שפעיל בתור אי־זוגי הוא מי שהתחיל.
- `pendingTriggers`, `castingIid` — תמיד ריקים כשמישהו מחזיק עדיפות.
- `deathtouched`, `combatDamageAssigned`, `delved` — חיים בחלון שבו אף אחד לא מקבל עדיפות.
- `abilityIndex` תמיד 0 ו־`abilityContext` תמיד `{}` — **נבדק מול הרישום החי**, לא מונח.
- ה־mode של Hullbreaker Horror נקרא מסוג המטרה שלו: לחש = מוד 0, פרמננט = מוד 1.
- `spellsCastThisTurn` — רק Veil of Summer קורא את זה, והשאלה שהוא שואל היא "הטילו כחול או שחור".
  בדק שבו כל לחש חוץ מ־Veil עצמו הוא כחול או שחור, ספירה מספיקה כדי לענות.

**ובראש הכול — `faithful()`:** לשחזר, להריץ `redact` על השחזור, ולהשוות מול ה־view שממנו הוא נבנה
(כולל רשימת הפעולות החוקיות). סוכן שנכשל בבדיקה **לא מחפש** בעמדה הזאת אלא נופל ליוריסטיקה. עלות:
`redact` אחד. תמורה: באג שחזור מתדרדר ל"שיחק את מהלך היוריסטיקה" ולא ל"חיפש משחק אחר".
נמדד על 400+ עמדות אמיתיות: **0 כשלונות**.

#### 4.2ב שני ממצאים

1. **§9.4 עובד בדיוק כפי שהמסמך אמר.** רק **5–8% מההחלטות** מקבלות חיפוש בכלל. חציון גורם
   ההסתעפות הוא 2, ורובן "pass, או לדפוק קרקע" — לא לבזבז עליהן כלום הוא מה שמשאיר תקציב לשאר.
2. **§13.1 בנוי ונכון, אבל כרגע לא מופעל.** פתרון משחק מטריצה מייצר תערובת רק כשהתשלומים
   **באמת מקיימים אינטראקציה**. מודל התשלומים של שלב 2 כמעט **ניתן להפרדה**: ה־Omniscience שלי
   שווה מה שהיד שלי שווה כמעט בלי קשר למה שנחת מולו, כי עם יד חיה אני מנצח בתור שלי והפרמננט
   שלהם לא מספיק לעשות כלום. מטריצה ניתנת להפרדה = שורה דומיננטית = אסטרטגיה טהורה.
   מה שיפעיל את זה הוא תשלום שיודע מה קורה כשתור הקומבו **נכשל** מול 7/7 מעופף — כלומר בדיוק
   ראש הערך של §10. עד אז: המנגנון במקום, והבדיקה מתעדת שהוא טהור ולמה.

#### 4.2ג המדידה, והמחיר שלה

| הרצה | תוצאה |
|---|---|
| 24 משחקים (5 דקות) | 54.2%, רווח סמך **29%–78%** — לא מחליט כלום |
| **400 משחקים (75 דקות, 4 חוטים)** | **61.0%, רווח סמך 54.1%–67.5%, +78 Elo — מובהק** |

שתי השורות האלה יחד הן כל מה ש־§5.3 מנסה לומר. אותו סוכן, אותו יריב, אותם seeds — ומ־24
משחקים היינו מסיקים "אולי קצת יותר טוב", ובעצם התשובה היא **יתרון אמיתי של כ־78 Elo**. מי שהיה
מסתפק ב־24 היה או פוסל שיפור אמיתי או מקדם רעש.

**מה זה כן קובע:** PIMC חזק יותר מהיוריסטיקה של שלב 1, מובהקית, עם **ארבע דטרמיניזציות בלבד**.

**מה זה עדיין לא קובע:** האם *יותר* חיפוש עוזר. אם `pimc:16` לא ינצח את `pimc:4`, אז ה־78 Elo
לא נקנו בחיפוש אלא במשהו אחר — וזו שאלה נפרדת שרצה על מכונה גדולה יותר.

**המחיר:**

| | |
|---|---|
| עלות playout אחד | **~100 מ"ש** |
| החלטות ב־rollout | ~500 (משחק מירור שלם, לא חצי) |
| משחק PIMC מול יוריסטיקה | **~45 שניות CPU** |
| 2,000 משחקים ב־`pimc:4` | **~6 שעות** על המכונה הזאת. `pimc:16` — פי ארבעה |

**למה playout עולה 100 מ"ש ולא 10.8 כמו ב־§9.3:** המסמך הניח rollout של ~100 החלטות ב־108µs.
בפועל rollout במירור הוא ~500 החלטות (משחקים נמשכים 18 סיבובים ו־652 החלטות),
וההחלטה עולה ~200µs על המכונה הזאת. פי חמישה מאורך המשחק, פי שניים מהמכונה.

**המסקנה:** §9.3 צודק לגמרי — **החלפת ה־rollout בהערכה היא הדבר בעל התשואה הגבוהה ביותר
שנשאר**. אבל עכשיו זה שיפור לדבר שכבר הוכח שעובד, ולא תנאי לכך שאפשר יהיה בכלל למדוד אותו.

**הערה על אורך המשחקים:** 185 מתוך 400 המשחקים נגמרו בספרייה ריקה, לא בחיים. שני סוכנים שיודעים
לא לשאוב את עצמם למוות מייצרים משחקי טחינה של 18 סיבובים, והספרייה היא מה שנגמר קודם. זה
מאשר מחדש שהמשאב האמיתי בפורמט הזה הוא הקלפים ולא החיים — וזה גם למה rollout כאן יקר.

---

## 5. תשתית: ממשק הסוכן, self-play, זירת הערכה

### 5.1 ממשק הסוכן

ממשק אחד לכל הסוכנים — יוריסטיקה, חיפוש, רשת, ואדם. זה מה שמאפשר להריץ כל שניים זה מול זה.

```ts
// src/ai/agent.ts
export interface Agent {
  readonly name: string;
  /** בחירת פעולה כשיש עדיפות. מקבל בדיוק את מה ששחקן רואה. */
  act(view: PlayerView, deadlineMs: number): Intent;
  /** מענה על שאלה. אותו חוזה. */
  respond(view: PlayerView, choice: ChoiceView, deadlineMs: number): ChoiceResponse;
  /** אופציונלי: נקרא בין משחקים בסדרה, לסיידבורד. */
  sideboard?(pool: CardPool, history: MatchState): DeckEntry[];
}
```

שלוש הערות שאינן מובנות מאליהן:

- **`deadlineMs` ולא `simulations`.** תקציב זמן הוא מה שקיים בזמן משחק אמיתי, וזה מה שצריך לכוון
  מולו. סוכן חיפוש מרחיב עד שנגמר הזמן (anytime).
- **אין `state`.** רק `view`. זו ע1, נאכפת על ידי הטיפוס.
- **`respond` נפרד מ־`act`.** המנוע כבר מפריד ביניהם, ומרחב הפעולות שונה מהותית (§8).

### 5.2 Self-play headless

```ts
// src/ai/arena.ts
playGame({ p1: Agent, p2: Agent, seed: number, budgetMs }): GameRecord
```

`GameRecord` הוא **(seed, startingPlayer, action log)** ותו לא. זו נקודה חשובה: המנוע דטרמיניסטי,
ולכן משחק שלם נשמר בכ־**4KB** במקום 200 × 12.5KB = 2.5MB של מצבים. יחס של 1:600. מאגר של מיליון
משחקים הוא 4GB ולא 2.5TB, וכל דגימה ממנו היא replay של 38 מ"ש. זה משנה את כל כלכלת האחסון של
האימון, וזה נכס שכבר קיים בקוד — `buildGame(meta, log)` בשרת עושה בדיוק את זה.

### 5.3 זירת הערכה

```
npm run ai:arena -- --a heuristic --b pimc:200 --games 2000 --bo 1
```

- Elo עם רווח סמך. **בדק קומבו השונות עצומה** — הפרש של 55% ניצחונות ב־200 משחקים אינו מובהק.
  מינימום 2,000 משחקים לכל השוואה, ו־10,000 להחלטות "האם לקדם גרסה".
- **זוגות מראה:** כל seed מנוגן פעמיים, פעם אחת עם כל צד מתחיל. מבטל את יתרון הפתיחה ומוריד
  שונות בערך פי 2. זה חובה, לא נחמד־שיהיה.
- **חבילות רגרסיה:** ה־`SCENARIOS` הקיימים (`showAndTell`, `omniscienceTurn`, `hullbreakerLock`,
  `bowmasterWar`, `bowmastersLoop`, `fetchBrainstorm`) הופכים לבדיקות התנהגות: "בעמדה הזאת סוכן
  שפוי עושה X". מהיר, דטרמיניסטי, ותופס נסיגות שה־Elo לא רואה.

---

## 6. האצת המנוע — התנאי לכל השאר

חיפוש הוא הכפלה של עלות ההחלטה במספר הסימולציות. ב־207µs, חיפוש של 400 סימולציות בעומק 30 עולה
**2.5 שניות למהלך** — גבולי בזמן משחק ואסון ב־self-play. זו העבודה בעלת התשואה הגבוהה ביותר בפרויקט
כולו, ולפני כל שורת ML.

### 6.1 מדוד: מצב headless בלי snapshot — פי 1.9, בחינם

`takeRollbackSnapshot` עושה `JSON.stringify(this.state)` בכל פעם ששחקן מקבל עדיפות, כדי ש־Esc יוכל
לחזור אחורה. **99µs מתוך 207.** ב־self-play ובחיפוש אין Esc.

```ts
Game.create({ ..., undoable: false })   // ברירת מחדל true, ללקוח
```

מדוד: 207µs → 108µs להחלטה, 26 → ~48 משחקים/שנייה. שינוי של שורות בודדות.

### 6.2 undo במקום clone

שכפול מצב (234µs) יקר מהחלטה שלמה, ולכן MCTS קלאסי שמשכפל לכל צומת אינו אפשרי כאן. שלוש חלופות,
לפי סדר עדיפות:

1. **Replay מהלוג.** להגיע לצומת בעומק *d* עולה *d* × 108µs. פשוט, אפס סיכון נכונות, וכבר מיושם
   בשרת. עבור עומק ≤ 30 זה 3.2 מ"ש — יקר אבל שפוי.
2. **Undo log** — לרשום את ה־mutations ההפוכות בכל צעד ולגלגל אחורה. מהיר בסדר גודל (מיקרו־שניות),
   אבל דורש משמעת בכל מוטציה במנוע ומסוכן לנכונות. **המלצה: רק אם 1 נמדד כלא מספיק.**
3. **Copy-on-write על מבני הנתונים** (Immer / persistent maps). מפחית את עלות השכפול לפרופורציונלית
   לשינוי. משנה את כל המנוע. לא כדאי.

**התוכנית:** להתחיל מ־(1), למדוד, ולעבור ל־(2) רק אם החיפוש חנוק. הסיבה: (1) לא יכול להכניס באג
חוקים, ו־(2) יכול.

### 6.3 מה נשאר ב־108µs

צריך פרופיילינג ממוקד לפני ניחוש, אבל החשודים לפי הקוד: פותר המאנה עם ה־backtracking (§9 ב־DESIGN.md),
מעבר ה־state-based actions, ו־`enumerateLegalActions`. **מטרה: 40µs.** אם היא מושגת, חיפוש של 800
סימולציות נכנס בשנייה.

### 6.4 מקביליות

`worker_threads` ב־Node ל־self-play — משחקים בלתי תלויים, מקביליות מושלמת. 8 ליבות × 48 = ~380
משחקים/שנייה ללא רשת. בדפדפן: Web Worker אחד לחיפוש כדי לא לחסום את ה־UI. חובה — חיפוש של שנייה
על ה־main thread יקפיא את הלוח.

---

## 7. ייצוג המצב (encoding)

זה המקום שבו 25 הקלפים משתלמים. הייצוג הוא **מדויק ולא מקורב**.

### 7.1 שכבת ספירות (הבסיס)

עבור כל אחד מ־25 שמות הקלפים, ספירה בכל אזור:

| אזור | שלי | של היריב |
|---|---|---|
| יד | ספירה מדויקת (0–4) | **וקטור אמונה** (§12), לא ספירה |
| שדה קרב | מדויק | מדויק |
| קברות | מדויק | מדויק |
| גלות | מדויק | מדויק |
| ספרייה | ספירה נגזרת | ספירה נגזרת |

25 × 5 × 2 = 250 מספרים, מנורמלים ל־[0,1] בחלוקה ב־4.

**התובנה:** ספירת הספרייה נגזרת (4 פחות מה שנראה) ולכן אינה מידע נוסף — אבל **לתת אותה במפורש חוסך
מהרשת ללמוד חיסור**. זה זול ומשפר התכנסות.

### 7.2 שכבת אובייקטים (למה שדורש פרטים)

ספירות מאבדות "האם ה־Atraxa הזה מוקש" ו"מה המטרות של הלחש הזה". לכן שדה הקרב והסטאק מיוצגים גם
כ**רצף טוקנים**, אחד לאובייקט:

```
token = embed(oracleId) + flags[tapped, summoningSick, attacking, mine]
        + counters + damage + (למי שייך) + (עבור סטאק: מטרות כ־pointer)
```

עד ~24 טוקנים מכסה כל עמדה מציאותית (נמדד: לוח טיפוסי הוא 4–10 אובייקטים).

### 7.3 סקלרים גלובליים

חיים ×2, סיבוב, phase/step (one-hot, 12), מי פעיל, למי עדיפות, מוליגנים שנלקחו ×2, land drops,
mana צף לפי צבע, קרקעות לא־מוקשות לפי צבע שהן יכולות להפיק, `omniscienceActive`, מספר לחשים שהוטלו
בתור, אורך הסטאק, מי על המשחק, מספר המשחק בסדרה.

### 7.4 שתי תכונות שהן יתרון לא הוגן — וזה בסדר

- **`knownTop`** — הלקוח כבר עוקב, באופן חוקי, אחרי קלפים שהשחקן *ראה* בראש הספרייה שלו
  (Brainstorm, surveil, Mystic Sanctuary), ומוחק את זה בכל ערבוב. זו זיכרון, לא מידע. **לתת אותה
  לרשת.** אדם שוכח; הסוכן לא, וזה הוגן בדיוק כמו לרשום פתקים.
- **היסטוריית הפעולות של היריב** — 16 הפעולות האחרונות כרצף טוקנים. שם חי כל האיתות: "הוא השאיר
  שתי קרקעות פתוחות ולא עשה כלום" הוא ה־feature שממנו נלמד לזהות Mana Drain מול בלוף.

### 7.5 סיכום ממדים

| רכיב | ממד |
|---|---|
| ספירות | 250 |
| סקלרים גלובליים | ~60 |
| knownTop | 25 (one-hot של עד 3 קלפים) + 3 |
| טוקני שדה קרב + סטאק | עד 24 × 48 |
| היסטוריית פעולות | 16 × 32 |

**כ־340 מספרים "שטוחים" + שתי סדרות טוקנים.** קטן. זה נכנס ברשת של 200–500K פרמטרים.

---

## 8. מרחב הפעולות ופונקציית המדיניות

### 8.1 הבעיה

הפעולות אינן הומוגניות. יש `castSpell` / `playLand` / `activateAbility` / `tapForMana` / `pass`,
ובנפרד תשובות לשאלות: `chooseCards` (**תת־קבוצה** בגודל min..max), `chooseTargets`, `chooseMode`,
`yesNo`, `orderTriggers`, `declareAttackers`, `declareBlockers`, `distributeDamage`, `secret`.
softmax בגודל קבוע לא מתאים לזה.

### 8.2 הפתרון: הרשת מדרגת את הרשימה החוקית

המנוע כבר **מייצר את רשימת האפשרויות החוקיות** בכל צומת. לכן:

```
score_i = MLP( encode(state) ⊕ encode(action_i) )
π = softmax over i
```

זהו pointer network. יתרונות שאינם מובנים מאליהם:

- **מרחב הפעולות פתור מעצמו.** אין מסכות, אין פעולות לא חוקיות, אין עונש על ניחוש לא חוקי.
- **מכליל למאגר הדראפט** — קלף שלא נראה באימון מקבל ציון דרך ה־embedding שלו, בלי לשנות ארכיטקטורה.
  זה מה שיאפשר לשלב ב' לרכב על אותה רשת.
- מספר הפעולות קטן (ממוצע 3) ולכן העלות זניחה.

### 8.3 בחירות של תת־קבוצה — פירוק אוטו־רגרסיבי

`chooseCards` עם `min=2, max=2` מתוך 7 הוא 21 אפשרויות; מתוך 53 (המקסימום שנמדד) הוא 1,378.
במקום למנות: **לבחור קלף אחד בכל פעם**, לקודד מחדש את המצב החלקי, ולבחור שוב, עד `stop` או `max`.

זה בדיוק איך שאדם בוחר, זה מייצר בעיית למידה נקייה, וזה מטפל גם ב־`orderTriggers` (סדר = בחירה
חוזרת) ו־`declareAttackers` (תת־קבוצה) באותו מנגנון. **מנגנון אחד לחמישה סוגי שאלות.**

### 8.4 הבחירה הסימולטנית

`simultaneousSecret` (Show and Tell) אינה החלטה רגילה — היא **משחק מטריצה**. אסור לתת לרשת לבחור
argmax שם: יריב שיודע מה תבחר מנצח. §13.1 מטפל.

---

## 9. חיפוש תחת מידע חלקי

### 9.1 שלב 2 — PIMC (Perfect Information Monte Carlo)

הפשוט ביותר שעובד:

1. דגום *K* **דטרמיניזציות**: השלמות של המידע הנסתר שעקביות עם כל מה שנראה — כלומר, חלק את
   הקלפים שלא נראו בין יד היריב וספריות שני הצדדים. (בזכות §2.1 זו דגימה **מדויקת**, לא הערכה.)
2. פתור כל אחת כמשחק מידע מלא (αβ / MCTS — בגורם הסתעפות 3 זה מהיר).
3. בחר את הפעולה עם הערך הממוצע הטוב ביותר.

**החולשות ידועות ואמיתיות:**

- **strategy fusion** — הסוכן מניח שיוכל לשחק אחרת בכל דטרמיניזציה, ולכן מעריך יתר על המידה מהלכים
  ש"עובדים אם מזל". בפועל: הוא לא ילמד להחזיק תשובות, כי בכל דטרמיניזציה הוא "יודע" אם הוא צריך.
- **non-locality** — הוא מתעלם מכך שהיריב היה משחק אחרת אילו היד שלו הייתה אחרת.
- **הוא לא מבלף ולא קורא בלוף.** לעולם.

למרות זאת, PIMC היה במשך שנים ה־state of the art בברידג' ובסקאט, וכאן — עם דק ידוע וגורם הסתעפות 3 —
הוא יהיה חזק. **זה השלב שבו יש יריב אמיתי, ולכן הוא נקודת ההחלטה.**

### 9.2 שלב 3 — IS-MCTS

עץ אחד על **קבוצות מידע** במקום עץ לכל דטרמיניזציה: כל איטרציה דוגמת דטרמיניזציה חדשה אבל חולקת
סטטיסטיקות. מרפא חלק מ־strategy fusion בעלות שולית מעל PIMC. תוספת טבעית, לא כתיבה מחדש.

### 9.3 שלב 4 — MCTS מונחה רשת

- **ראש מדיניות** גוזם: בגורם הסתעפות 3 זה פחות קריטי, אבל הוא מכוון את התקציב לצמתים החשובים.
- **ראש ערך** מחליף rollouts. **זו התרומה הגדולה.** rollout עד סוף המשחק הוא ~100 החלטות × 108µs
  = 10.8 מ"ש. הערכה ברשת היא ~20µs. **פי 500.** זה ההבדל בין 90 סימולציות לשנייה לבין 9,000.

### 9.4 הקצאת תקציב לפי חשיבות הצומת

בגלל §2.3 — רוב הצמתים טריוויאליים — התקציב צריך להיות לא־אחיד:

```
budget(node) = base × f(entropy(π), |legalActions|, valueSpread)
```

צומת עם פעולה חוקית אחת: 0 סימולציות. צומת שבו ראש הערך נותן פיזור רחב על הפעולות: תקציב מלא.
בפועל זה שווה־ערך לפי 5–10 בעומק החיפוש האפקטיבי בצמתים שחשובים, בלי עלות נוספת. **אופטימיזציה
ממדרגה ראשונה כאן, בגלל פרופיל המשחק הספציפי הזה.**

---

## 10. ארכיטקטורת הרשת

```
              ┌─ ספירות + סקלרים (340) ──┐
              │                           ├─→ MLP 2×256 ─┐
              └─ knownTop ────────────────┘               │
                                                          ├─→ trunk (256) ─┬─→ value  (1, tanh)
   טוקני שדה קרב/סטאק (≤24×48) ─→ Transformer 2×128 ─────┤                 ├─→ policy (scorer)
                                                          │                 ├─→ aux: יד היריב (25)
   היסטוריית פעולות (16×32) ────→ Transformer 1×128 ──────┘                 └─→ aux: אורך המשחק
```

- **גודל: ~400K פרמטרים.** 1.6MB ב־fp32, ~400KB ב־int8.
- **למה טרנספורמר קטן ולא CNN:** אין מבנה מרחבי. יש **קבוצה** של אובייקטים בגודל משתנה. attention
  הוא ההתאמה הנכונה, ובגודל הזה הוא זול.
- **למה לא רשת ענקית:** אין מה להכליל עליו. 25 קלפים, כל אחד עם embedding משלו. קיבולת נוספת תשנן
  רעש, ורשת גדולה לא תרוץ בדפדפן.
- **ראשי עזר** — לחזות את יד היריב ואת אורך המשחק. הם לא משמשים בזמן משחק; הם קיימים כי הם מכריחים
  את ה־trunk ללמוד ייצוג של אמונה, וזה מאיץ למידה של הערך. זה גם מה שמייצר בחינם את מודל האמונה של §12.

---

## 11. לולאת האימון

### 11.1 השלד (AlphaZero, מותאם למידע חלקי)

```
loop:
  1. self-play: N משחקים, MCTS מונחה רשת, טמפרטורה יורדת
  2. שמור (info-state, π_MCTS, z) — כ־(seed, log) + אינדקסים
  3. אמן: L = CE(π_net, π_MCTS) + MSE(v, z) + λ·L_aux
  4. הערך מול הגרסה הקודמת ומול הסולם; קדם אם Elo עלה מובהקית
```

### 11.2 ההתאמה למידע חלקי

ב־AlphaZero הערך הוא של *מצב*. כאן הוא של **קבוצת מידע**, וזה שינוי מהותי:

- מטרת הערך היא התוצאה מהמשחק שנוגן — משוערך רועש של הערך האמיתי של קבוצת המידע.
- **התיקון:** לדגום כמה דטרמיניזציות מאותה קבוצת מידע ולמצע. יקר אך מוריד שונות משמעותית.
- **חלופה זולה יותר:** לאמן על התוצאה, ולסמוך על כך שהממוצע על פני מיליוני משחקים מתכנס לערך של
  קבוצת המידע. עובד, מתכנס לאט יותר. **להתחיל בזה.**

### 11.3 ליגה, לא סולם יחיד

בדק קומבו מראה יש דינמיקת אבן־נייר־ומספריים אמיתית: "להתרוקן מהר" מנצח "להחזיק תשובות" שמנצח
"להתרוקן איטי". אימון מול הגרסה האחרונה בלבד ייצור מעגליות והתמחות יתר.

**ליגה בסגנון AlphaStar:** אוכלוסייה של סוכנים — main agents, exploiters (מאומנים לנצח את ה־main),
ו־past agents קפואים. יריב נדגם מהאוכלוסייה. עולה יותר, ומונע את מצב הכישלון הכי סביר בפרויקט הזה.

### 11.4 היפר־פרמטרים כנקודת פתיחה

| | ערך | נימוק |
|---|---|---|
| סימולציות MCTS ב־self-play | 200 | פשרה בין איכות התווית לקצב |
| מקדם Dirichlet בשורש | α=0.6 | מותאם לגורם הסתעפות ~3 (α≈10/b) |
| טמפרטורה | 1.0 ל־12 החלטות ואז 0 | 12 ≈ תור אחד ומשהו |
| Batch | 1024 | |
| קצב למידה | 2e-3 קוסינוס | רשת קטנה, אפשר להיות אגרסיבי |
| Replay buffer | 500K עמדות, חלון גולש | |
| קצב דגימה | כל עמדה ~4 פעמים בממוצע | |

---

## 12. מודל אמונה ומודל יריב

זה מה שמפריד סוכן טוב מסוכן חזק במשחק הזה, ובזכות §2.1 הוא **בר־חישוב מדויק**.

### 12.1 מסנן חלקיקים על יד היריב

מצב: התפלגות על רב־קבוצות אפשריות של היד. עדכון:

1. **גזירה קשיחה:** 60 פחות מה שנראה = מה שנשאר. יד בגודל *h* היא תת־קבוצה של זה. חסם עליון מדויק
   על כל שם קלף.
2. **גזירה רכה (הדבר האמיתי):** *מה שהוא לא עשה*. אם הייתה לו עדיפות ולחש שלי היה על הסטאק והוא לא
   קיאנטר — ההסתברות ש־Mana Drain בידו יורדת. זה בדיוק Bayes: `P(hand|actions) ∝ P(actions|hand)·P(hand)`,
   ו־`P(actions|hand)` הוא **מודל היריב**.

### 12.2 מאיפה מגיע מודל היריב

- **בהתחלה:** הסוכן עצמו. `P(action|hand)` מהרשת שלו. הנחת עצמי־כיריב, בדיוק כמו self-play.
- **מול אדם:** להתאים מקוון. אדם שלא מקאנטר לחשים זולים הוא מידע שנצבר תוך 3 משחקים בסדרה.
  שדרוג טבעי אחרי שהבסיס עובד; **לא בגרסה הראשונה** — קל לעשות אותו רגיש מדי לרעש.

### 12.3 ההצדקה

ראש העזר של §10 (חיזוי יד היריב) נותן את זה כמעט בחינם: הרשת כבר מאומנת להסיק את היד מההיסטוריה.
המסנן החלקיקי הוא הגרסה המפורשת והמדויקת; ראש העזר הוא הגרסה המהירה. **להשתמש בראש בזמן משחק,
ובמסנן כדי לדגום דטרמיניזציות לחיפוש.** זו חלוקת עבודה נכונה.

---

## 13. נקודות שהן באמת תורת משחקים

Best response הוא לא המטרה. סוכן שתמיד משחק את התגובה הטובה ביותר להתפלגות מסוימת הוא **ניצול**
במקומות הבאים:

### 13.1 הבחירה ב־Show and Tell

משחק מטריצה סימולטני, 3–5 אפשרויות לכל צד (Omniscience / Atraxa / Hullbreaker / כלום). התשלומים
מוערכים על ידי ראש הערך של הרשת. **מטריצה של 5×5 נפתרת ל־Nash מדויק בזמן זניח** (LP או fictitious play).

זו הזדמנות חינם: זו הנקודה החשובה ביותר במשחק, היא זעירה, והיא ניתנת לפתרון מדויק במקום לקירוב.
**לעשות את זה כבר בשלב 2**, לא לחכות ללמידה.

### 13.2 להחזיק מאנה — לבלף או לא

"להשאיר UU פתוח בלי Mana Drain" הוא בלוף אמיתי. מדיניות דטרמיניסטית ניתנת לניצול: יריב שלומד שאתה
משאיר מאנה רק כשיש לך תשובה — פשוט מתעלם. הפתרון הוא **מדיניות מעורבת**, וזה מה ש־CFR על תת־המשחק
נותן.

### 13.3 CFR על תת־משחקים (שלב 5)

לא לפתור את המשחק כולו — לזהות את הצמתים הקריטיים ולפתור **תת־משחק** סביבם, בסגנון ReBeL/DeepStack:
ערך העלים מהרשת, אמונה מהמסנן, CFR בפנים. מגיע קרוב לאיזון בנקודות שחשובות ומשאיר את השאר לרשת.

**זה השלב שבו הסוכן מפסיק להיות ניתן לניצול, וזה הפירוש המדויק של "למקסם את היכולות".**

---

## 14. הערכה: איך יודעים שהוא באמת חזק

| מדד | איך | למה |
|---|---|---|
| **Elo מול הסולם** | 2,000+ משחקים, זוגות מראה | התקדמות יחסית |
| **Exploitability** | לאמן best-response לסוכן הקפוא ולמדוד בכמה הוא מנצח | **המדד היחיד שמשמעותי במידע חלקי** |
| **חבילת עמדות** | ה־SCENARIOS כבדיקות התנהגות | רגרסיה מהירה |
| **מול אדם** | סדרות Bo3 מתועדות | הדבר האמיתי |
| **שפיות** | האם הוא מקאנטר את Show and Tell? מוליגן יד של קרקע אחת? | תופס אימון שהתפרק |

**Exploitability הוא המדד המרכזי.** סוכן שמנצח 95% מול היוריסטיקה יכול להיות ניתן לניצול ב־80%
על ידי יריב שמותאם אליו. במידע חלקי, "מנצח את מי שאימנתי מולו" הוא כמעט חסר משמעות בלעדיו.

---

## 15. דרגות קושי וחוויית שחקן

טעות נפוצה: להחליש AI על ידי הוספת מהלכים אקראיים. התוצאה משחקת **מוזר**, לא חלש — היא עושה טעויות
שאדם לעולם לא עושה, וזה מתסכל.

**הגישה הנכונה — סוכן חזק עם תקציב קטן:**

| דרגה | מנגנון |
|---|---|
| מתחיל | יוריסטיקה (שלב 1) בלבד |
| בינוני | חיפוש, 30 סימולציות, טמפרטורה 0.7 |
| קשה | 300 סימולציות, טמפרטורה 0.25 |
| מקסימלי | תקציב מלא, טמפרטורה 0, פתרון תת־משחקים דלוק |

סוכן עם תקציב קטן טועה **טעויות אנושיות** — מפספס לינה עמוקה, לא מעריך נכון מרוץ. זה יריב מהנה.

**שקיפות:** להראות את דרגת הקושי ואת תקציב החשיבה. וכן — **הסוכן חייב לרוץ מול אותו `redact`**,
כך שאפשר לומר לשחקן בכנות שהוא לא רואה את הקלפים שלך. זה נכס אמון, וזה כבר נאכף ארכיטקטונית.

---

## 16. פריסה: להריץ את זה בדפדפן

האפליקציה היא לקוח סטטי + serverless. שתי אפשרויות:

| | בדפדפן | בשרת |
|---|---|---|
| השהיה | 0 | 100–300 מ"ש לכל מהלך |
| עלות | 0 | לכל מהלך, לכל משחק |
| עובד אופליין | כן | לא |
| גודל | +400KB–1.6MB הורדה חד־פעמית | 0 |

**המלצה חד־משמעית: בדפדפן.** הפרויקט כבר עובד אופליין (קלפי טקסט כשאין רשת), ה־serverless כבר על
תקציב חינם, והשהיה של 300 מ"ש למהלך במשחק של 200 החלטות היא דקה שלמה של המתנה מצטברת.

- **inference:** onnxruntime-web (WASM SIMD) או matmul ידני. רשת של 400K פרמטרים = כ־20µs למעבר.
  בגודל הזה, ידני עשוי לנצח — בלי 2MB של runtime.
- **החיפוש ב־Web Worker.** חובה. המנוע כבר טהור ודטרמיניסטי, ולכן ניתן להריץ אותו ב־worker בלי DOM.
- **קוונטיזציה ל־int8** אם ההורדה מציקה. בגודל הזה כנראה מיותר.

---

## 17. שלב ב' — הדראפט

### 17.1 החסם, לפני כל תכנון

**מתוך 69 קלפי המאגר, ל־2 יש סקריפט.** כלומר: קלפים שנדראפטו לא ניתנים להטלה. המסקנה בלתי נמנעת —

> **אי אפשר ללמוד ערך של קלף מ־self-play כשהקלף לא ניתן לשחק.**

כל תוכנית לדראפט חייבת לפתוח מכאן. שני מסלולים:

**מסלול א' — לממש את הסקריפטים החסרים.** 67 קלפים. לפי הקצב בפועל של הפרויקט (27 קלפים מומשו),
זו עבודה של 3–5 שבועות, מכנית ברובה, עם בדיקות. אחריה הדראפט הופך ללמיד באמת: ערך קלף = כמה הוא
מעלה את סיכויי הניצחון, נמדד ב־self-play. **זו הדרך היחידה לדראפטר חזק באמת.**

**מסלול ב' — ערכים חיצוניים, מדיניות נלמדת.** לקבע ערכי קלפים מ־prior חיצוני (דירוגי לימיטד, הערכת
LLM, או ידנית), וללמוד רק את **מדיניות המכרז**. זול, נותן דראפטר סביר תוך ימים, אבל הערכים אינם
נלמדים מהמשחק ולכן מפספסים סינרגיות עם המירור — שהן בדיוק מה שמעניין בפורמט הזה.

**ההמלצה:** ב' עכשיו, א' כמסלול. ובאמצע — **לממש את 15–20 הקלפים בעלי ההשפעה הגדולה ביותר**
ולהתייחס לשאר כ־vanilla. זה קונה את רוב האות בחלק מהעלות.

### 17.2 הפירוק: ערך × מכרז

הדראפט מתפרק לשתי בעיות בלתי תלויות כמעט, וזו התובנה המרכזית כאן:

```
מדיניות דראפט = f( ערך(קלף | הדק שנבנה עד כה) , אסטרטגיית מכרז(מטבעות, ערמות שנותרו) )
```

**החצי השני ניתן לפתרון כמעט מדויק.** מצב המכרז הוא:

```
(מטבעות שלי 0–24, מטבעות שלו 0–24, ערמות שנותרו 0–17, הצעה נוכחית, מי מציע, ערך הערמה המשוער)
```

25 × 25 × 18 × ~25 × 2 = כ־560,000 מצבים עם ערך ערמה מדיסקרטי. **זה נפתר בתכנון דינמי טבלאי.**
עם מידע פרטי (כל שחקן רואה קלף אחד שהיריב לא) זה מכרז בייסיאני — CFR על עץ המכרז יגיע לאיזון קרוב.

זה שווה הרבה: **מחצית מבעיית הדראפט ניתנת לפתרון כמעט אופטימלי בלי ללמוד כלום**, ברגע שיש ערכים.

### 17.3 המושג המרכזי: ערך האופציה של מטבע

הטעות הכי גדולה בדראפט הזה היא להוציא 8 על ערמה טובה בערמה 2 מתוך 17. מטבע אינו רק כוח קנייה —
הוא **אופציה** להתחרות על כל ערמה עתידית.

```
ערך שולי של מטבע = ∂E[ערך הדק הסופי] / ∂מטבעות
```

הוא **עולה** ככל שנותרו ערמות ו**קורס לאפס בערמה האחרונה** — שם ההצעה הנכונה היא כל מה שנשאר, אם
הערמה שווה משהו. ה־DP של §17.2 מייצר את העקומה הזאת ישירות. הממשק כבר מציג "ערך ערמה ממוצע"
(סך המטבעות ÷ ערמות שנותרו), שהוא הקירוב מדרגה־אפס לאותו דבר; ה־DP הוא הגרסה הנכונה.

### 17.4 הערכת קלפים בהקשר

ערך של קלף אינו סקלר. הוא תלוי במה שכבר נבנה: Force of Will רביעי שווה פחות מהראשון; ערך הסייד־בורד
תלוי במה שהיריב לקח (וזה נראה חלקית!).

```
value(card | myPool, theirPool, coinsLeft) = E[winrate | לקחתי אותו] − E[winrate | לא]
```

עם מסלול א' זה נמדד. עם מסלול ב' זה מקורב כ־`base(card) + synergy(card, myPool)` עם מטריצת סינרגיה
קטנה (69×69, נלמדת או ידנית).

### 17.5 בניית הדק

מהקלפים שנזכו + המירור + 16 הקרקעות המחולקות, לבנות 60. זו בעיית אופטימיזציה בדידה קטנה:
חיפוש מקומי על עקומת המאנה ומקורות הצבע, מוערך על ידי ראש הערך של המשחק. **פתיר בשלמות בהינתן
פונקציית הערכה.**

### 17.6 שלבי הדראפט

| שלב | מה | תלוי ב |
|---|---|---|
| ד0 | מנוע דראפט headless + זירה (המנוע כבר טהור ודטרמיניסטי) | — |
| ד1 | ערכי קלפים חיצוניים + מכרז חמדני | ד0 |
| ד2 | **פותר מכרז ב־DP** בהינתן ערכים | ד1 |
| ד3 | CFR על עץ המכרז (המידע הפרטי) | ד2 |
| ד4 | מימוש 15–20 הקלפים המשמעותיים; ערכים נלמדים לתת־הקבוצה | סקריפטים |
| ד5 | 67 הסקריפטים; ערך קלף נלמד מ־self-play מקצה לקצה | סקריפטים |

**ד2 הוא ה־sweet spot.** הוא נותן מכרז כמעט אופטימלי בעלות נמוכה, והוא בלתי תלוי לחלוטין בחסם
הסקריפטים.

---

## 18. תקציב חישוב ולוח זמנים

### 18.1 החישוב שקובע

בהנחת 108µs להחלטה (אחרי §6.1) ו־20µs להערכת רשת:

```
עלות סימולציית MCTS ≈ 108µs (צעד מנוע) + 20µs (רשת) ≈ 128µs
מהלך ב־200 סימולציות     ≈ 26 מ"ש
משחק self-play (200 מהלכים) ≈ 5.2 שניות
```

| מכונה | משחקים/יום |
|---|---|
| ליבה אחת | 16,600 |
| 8 ליבות | **133,000** |
| 32 ליבות (VM בענן) | 530,000 |

**מיליון משחקי self-play ב־8 ליבות = שבוע.** למשחק בגודל הזה זה ככל הנראה יותר ממספיק. אין צורך
ב־GPU לאימון — רשת של 400K פרמטרים מתאמנת על CPU, ו־GPU צנוע יקצר את שלב האימון לדקות.

**המסקנה: זה פרויקט שרץ על מכונה אחת.** וזה בזכות §1, לא בזכות אופטימיות.

### 18.1א מה שנמדד בפועל — והתיקון להנחת "×ליבות"

ההנחה של 8 ליבות → פי 8 היא ההנחה היחידה בפרק הזה שלא שרדה מדידה. המשחקים באמת בלתי־תלויים
לחלוטין ולא חולקים דבר, ובכל זאת על המחשב שעליו זה נכתב (i5-1335U, 12 חוטים לוגיים, 15W):

| חוטים | משחקים/שנייה (1,200 משחקים, heuristic מול random) | יחס |
|---|---|---|
| 1 | 22 | ×1.0 |
| 4 | **46** | **×2.1** |
| 8 | 45 | ×2.0 |
| 10+ | לא יותר | — |

**הסיבה אינה תחרות על נעילה — אין נעילות ואין מצב משותף — אלא רוחב פס לזיכרון.** המנוע מקצה
הרבה (`redact` בונה 7.4KB בכל אחת מ־210 ההחלטות), ורוחב פס הוא בדיוק הדבר שיותר חוטים לא נותנים
יותר ממנו. הברך היא ב־4 חוטים על מכונה של 12.

**שני דברים שהופכים את המדידה הזאת לקשה יותר ממה שהיא נראית, ושניהם נותנים את אותה תשובה
שגויה — "חוטים לא עוזרים":**

- **חניקה תרמית.** מעבד נייד יורד בתדר תוך דקות של עומס מתמשך, ולכן מעבר יחיד על מספרי החוטים
  מודד את התדר יורד לפחות באותה מידה שהוא מודד מקביליות: מי שרץ ראשון מנצח. חובה לסרוק בשני
  הכיוונים ולקחת את המקסימום. במדידה שלמעלה המעבר הראשון נתן 27 ל־8 חוטים והשני נתן 45 — אותו
  קוד, אותם מספרים, מכונה קרה יותר.
- **עלות הקמת worker.** תחת loader של TypeScript זה כמעט שנייה לכל worker, וכמה שעולים בבת אחת
  מתחרים על אותן ליבות. בריצה של 80 משחקים זה כל המדידה; בריצה של 1,200 זה רעש. זו גם הסיבה
  האמיתית שריצת זירה קצרה כמעט לא מרוויחה מחוטים וארוכה כן.

המסקנה המעשית: **המספר שכדאי להאמין לו הוא זה ש־`npm run ai:arena` מדפיס בסוף ריצה אמיתית**,
לא מיקרו־בנצ'מרק.

**המשמעות לתכנון, בכנות:**

- שלבים 0–2 רצים כאן בנוחות. ריצת זירה של 2,000 משחקים היא כ־45–80 שניות.
- שלב 4 לא. מיליון משחקי self-play מונחי־MCTS ב־5.2 שניות למשחק הם כ־60 יום על המכונה הזאת,
  לא שבוע. מי שרוצה את שלב 4 צריך VM עם הרבה ליבות **ורוחב פס לזיכרון**, ולא לסמוך על ספירת ליבות.
- אם הצוואר באמת רוחב פס ולא CPU, אז §6.2 (undo log במקום שכפול) שווה יותר ממה שנראה: הוא מוריד
  הקצאות, לא רק מיקרו־שניות.

### 18.2 לוח זמנים

| שבוע | תוצר |
|---|---|
| 1 | שלב 0 + §6.1 (headless, פי 1.9) + זירה |
| 2 | שלב 1 — יוריסטיקה. **יריב ראשון שאפשר לשחק נגדו.** |
| 3 | שלב 2 — PIMC + Nash ל־Show and Tell (§13.1). **נקודת ההחלטה.** |
| 4 | שלב 3 — IS-MCTS, מדידת exploitability |
| 5–7 | שלב 4 — encoding, רשת, self-play, ליגה |
| 8–9 | כוונון, קוונטיזציה, פריסה בדפדפן, דרגות קושי |
| 10–11 | שלב 5 — CFR על תת־משחקים |
| 12+ | דראפט: ד0–ד3 |
| מקביל | מסלול הסקריפטים (ד4–ד5) |

---

## 19. סיכונים ומלכודות

| סיכון | חומרה | מה עושים |
|---|---|---|
| **המנוע איטי מדי לחיפוש** | גבוהה | §6. למדוד אחרי §6.1 לפני שממשיכים. אם 40µs לא מושג — undo log. |
| **67 קלפי מאגר בלי סקריפט** | גבוהה (לדראפט) | §17.1. להצהיר על התקרה מראש; לא להעמיד פנים שלומדים ערכים. |
| **שונות מסתירה התקדמות** | גבוהה | 2,000+ משחקים, זוגות מראה. אחרת יקודמו גרסאות גרועות. |
| **מעגליות בליגה** | בינונית | אוכלוסייה + exploiters (§11.3), לא סולם יחיד. |
| **PIMC "מספיק טוב" ושלב 4 לא מוצדק** | בינונית | זו תוצאה לגיטימית. למדוד ב־exploitability ולהחליט בכנות. |
| **הסוכן לומד לנצל באג במנוע** | בינונית | קרה בכל פרויקט RL אי פעם. הסוויטה (274 בדיקות) היא ההגנה; לבדוק ידנית משחקים מנצחים. |
| **פיתוי ל־reward shaping** | בינונית | ע2. שיפינג בדק קומבו מלמד "לגרום נזק" במקום "לנצח". |
| **דליפת מידע דרך העיתוי** | נמוכה | הבעיה קיימת בקוד ומטופלת (חלון אקראי ל־auto-pass). לוודא שהסוכן לא חושף מידע דרך *זמן החשיבה*. |
| **התמכרות ל־overfitting על ה־drills** | נמוכה | הם רגרסיה, לא אימון. לא לאמן עליהם. |
| **הסוכן שואב את הספרייה שלו ומת** | ~~בינונית~~ **קרה** | §4.1(1). לא מספר קסם אלא תנאי: לחפש רק כל עוד יש מה למצוא. `worthDrawing`. |
| **לולאה שלא מסתיימת** | ~~נמוכה~~ **קרה** | §4.1(2). הזירה מסמנת `unfinished` ומדפיסה "investigate"; אסור להתייחס לזה כתיקו. |
| **מקביליות פחות טובה מ־×ליבות** | ~~נמוכה~~ **קרה** | §18.1א. למדוד, לסרוק בשני הכיוונים בגלל חניקה תרמית, ולא להסיק מספירת ליבות. |

---

## 20. נספח: החלטות שכבר סגורות

הדברים שאין צורך לדון בהם שוב:

1. **הסוכן צורך `redact`, לא `state`.** נאכף בטיפוסים ובבדיקה.
2. **התגמול הוא ניצחון/הפסד.** שיפינג רק כראשי עזר.
3. **מדיניות = דירוג הרשימה החוקית**, לא softmax בגודל קבוע (§8.2).
4. **בחירות תת־קבוצה נפתרות אוטו־רגרסיבית** (§8.3).
5. **משחקים נשמרים כ־(seed, log)**, לא כמצבים. יחס 1:600.
6. **Show and Tell נפתר כמשחק מטריצה**, לא ב־argmax (§13.1).
7. **קושי = תקציב חיפוש**, לא רעש (§15).
8. **פריסה בדפדפן, ב־Web Worker** (§16).
9. **המכרז בדראפט הוא בעיה נפרדת מהערכת קלפים**, והוא כמעט פתיר (§17.2).
10. **Exploitability הוא המדד**, לא Elo מול היוריסטיקה (§14).

---

## נקודות ההחלטה, בשורה אחת כל אחת

- **אחרי §6.1:** האם המנוע מהיר מספיק? אם לא — undo log לפני כל ML.
- **אחרי שלב 2:** האם PIMC כבר מספיק חזק? למדוד exploitability ולהחליט בכנות אם שלבים 4–5 מוצדקים.
- **לפני הדראפט:** האם מממשים 67 סקריפטים? זו ההחלטה היקרה ביותר במסמך, והיא לא טכנית — היא על
  כמה חשוב שהדראפט יהיה חכם באמת.
