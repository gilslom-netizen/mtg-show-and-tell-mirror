node_modules/
dist/
*.log
.DS_Store
data/*.sqlite
data/matches/

# Arena output. A checkpoint is a run in progress; a result is what it concluded.
# Both are measurements of a particular machine on a particular day, so they are
# reported rather than committed.
checkpoints/
results/
